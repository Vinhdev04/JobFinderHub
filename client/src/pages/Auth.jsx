import React from 'react';
import AuthForm from '@features/auth/components/AuthForm';

function Auth() {
    return (
        <div>
            <AuthForm />
        </div>
    );
}

export default Auth;
